const { getExpectedChangeValues } = require('./general');

const {
    getMockLead,
    getMockLeads
} = require('./mockLeads');


module.exports = {
    getExpectedChangeValues,
    getMockLead,
    getMockLeads
}